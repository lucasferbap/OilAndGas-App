export class Sort{
    sorted : Boolean;
    unsorted : Boolean;
    empty : Boolean;

    constructor(){
        this.sorted = new Boolean();
        this.unsorted = new Boolean();
        this.empty = new Boolean();
    }
}